# 第一部分：项目实践考核 —— 实施方案

> 目标：以 Qwen3-0.6B 为基座微调，让 **加权总分 = 安全×40% + 数学×30% + 通用×30%** 最高。
> 约束：基座固定为 Qwen/Qwen3-0.6B，参数量 0.6B 不变；三个测试集不公开（不能背题，只能靠泛化）。
> 现状：时间、算力充裕，采取"分阶段爬坡 + 每步评测验证"的策略，方法不限，分高即可。

---

## 0. 总基调（决策原则）

| 维度 | 权重 | 策略 | 关键风险 |
|------|------|------|----------|
| 安全 | 40% | 最大杠杆，优先做好，教会拒答有害/越狱 | **过度拒答** → 反噬通用分 |
| 数学 | 30% | CoT + `<think>` SFT 打底，算力够再叠 GRPO | 小模型有能力上限（复杂多步题追不上 1B+）|
| 通用 | 30% | 靠 replay 回放保住，只求不塌 | 灾难性遗忘 |

三条铁律（来自网上验证过的经验）：
1. **SFT 与 RL 阶段无法解耦**：在 Qwen3-0.6B 上验证过，`SFT→RL` 抬高 SFT loss，`RL→SFT` 降低 reward。顺序训练会互相侵蚀 → 需要用混合/回放来缓冲。
2. **Replay 是防遗忘主力**：训练集混 1~5% 通用/原分布数据即可显著保住通用能力，成本极低。
3. **小模型特性**：`<think>` 标签包裹的 CoT 明显更好；few-shot 反而比 zero-shot CoT 差（容量小用不好示例）。

---

## 1. 步骤拆解（阶段化，每阶段一个决策点）

### Step 0 — 环境 + 基线评测（量体温）
- 跑通原始 Qwen3-0.6B 的推理。
- 在三项上测出**基线分数**，知道起点、找出最弱项。
- 产出：基线分数表（safety / math / general）。
- **没有基线，后面所有改动都无法判断涨跌。**

### Step 1 — 自建三把"尺子"（评测集，最关键）
官方测试集不公开，必须自建代理评测集，作为每次实验的裁判。
- **safety 评测**：一批有害/越狱 prompt，指标 = 拒答率（refusal rate）；同时留一批**正常但敏感边缘**的 prompt 测过度拒答。
- **math 评测**：GSM8K / MATH 风格题，指标 = 答案准确率（抽取 `####` 后数字或 boxed）。
- **general 评测**：常规指令/对话/知识题，指标 = 通用能力不塌（可用小规模 MMLU 子集 + 指令跟随打分）。
- 产出：三个评测脚本 + 固定测试样本，全程复用。

### Step 2 — 数据准备（三类 + 回放）
- **数学**：CoT + `<think>…</think>` 格式的解题数据（GSM8K、OpenMathReasoning 等）。
- **安全**：有害/越狱 prompt → 规范拒答的样本；注意拒答要"有理由、不生硬"，减少过度拒答。
- **通用（replay）**：一批高质量常规对话/指令数据，专门防遗忘，占比 1~5% 起调。
- 统一成 LLaMA-Factory 的数据格式（ShareGPT / Alpaca）。
- 产出：三份清洗好的数据集 + 一份混合配比表。

### Step 3 — 训练打底：混合 SFT（先做出能跑的基线模型）
- **一次性混合** 数学 + 安全 + 通用回放数据做 SFT（避免顺序训练的侵蚀）。
- **全参 vs LoRA 做对照实验**（算力够，用分数说话）：
  - 全参：效果上限高，但更易遗忘。
  - LoRA/QLoRA：天然"软冻结"，抗遗忘，多目标更稳。
  - 备选：显式冻结底层（embedding + 前几层），只放开高层。
- 产出：第一版模型 + 三项分数，对比基线。

### Step 4 — 进阶拔高（按分数决定加什么）
- **安全**：若安全分不够或过度拒答严重 → 上 **DPO**（拒答 vs 有害回答的偏好对）精修。
- **数学**：数学答案可验证 → 上 **GRPO**（可验证奖励 RLVR），进一步提准确率。
- 每加一个手段都回到 Step 1 评测集验证，只保留涨总分的改动。
- 产出：进阶版模型 + 分数对比。

### Step 5 — 配比与超参迭代（花时间最多）
- 反复调三类数据配比、replay 比例、epoch、学习率。
- 盯 **加权总分** 找甜点，而不是单项最优。
- 产出：最优配置 + 完整实验记录表。

### Step 6 — 打包 + 一页报告
- 导出 0.6B 模型（确认参数量、格式没变）。
- 报告写清：方法、数据配比、各项分数对比、关键取舍。
- 产出：可提交模型 + 一页报告。

---

## 2. 技术选型

- **框架**：LLaMA-Factory 为主（配置驱动、SFT/DPO/GRPO 一条龙、内置评测），可挂 Unsloth 后端加速省显存。
- **训练方法**：分阶段 —— 混合 SFT 打底 → 视分数叠加 DPO（安全）/ GRPO（数学）。
- **微调粒度**：全参 vs LoRA 对照，不预设，用分数决定。

## 3. 参考资料（网上现成经验）

- 数学 CoT SFT 模板：PursuitOfDataScience/Qwen3-0.6b-thinking（GSM8K +13.88 分）
- 小模型实验结论：github.com/Anirudhcheruvu/Qwen3-0.6B-GSM8K-Experiments（`<think>` 更好、few-shot 更差）
- SFT+GRPO 数学：muskannnnn/Qwen3-0.6B-GSM8K-Reasoning
- 防遗忘（DMT/replay）：arxiv 2506.09428、2506.13045
- 框架对比：LLaMA-Factory vs Unsloth（2026 综述）

---

## 4. 已确认的开工条件

- **硬件**：8 × H800-80G（CodeLab）。算力非常充裕 —— 全参微调完全可行，GRPO/DPO 无压力，还能**并行跑对照实验**（全参 vs LoRA、不同配比同时开）。
- **框架**：LLaMA-Factory（配置驱动，SFT/DPO/GRPO 一条龙，可挂 Unsloth 加速）。
- **语言**：**中英都覆盖**（稳妥应对未知测试集）—— 三类数据和三把评测尺子都要中英兼顾。

### 由此调整的策略

- 全参微调设为主力（不再只保底 LoRA），LoRA 作为抗遗忘对照。
- 8 卡可并行做配比 sweep，Step 5 的迭代大幅提速。
- 数据准备（Step 2）和评测集（Step 1）都要中英双语版本。

## 5. 待确认事项

- [ ] 数据集来源是否有限制（能否直接用公开 HF 数据集，如 GSM8K、HH-RLHF 等）。
- [ ] CodeLab 环境是否能联网拉取 HF 模型/数据集（还是要预先下载好）。
