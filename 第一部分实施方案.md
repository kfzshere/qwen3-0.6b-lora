# 第一部分：项目实践考核 —— 实施方案

> 目标：以 Qwen3-0.6B 为基座微调，让 **加权总分 = 安全×40% + 数学×30% + 通用×30%** 最高。
> 约束：基座固定为 Qwen/Qwen3-0.6B，参数量 0.6B 不变；三个测试集不公开（不能背题，只能靠泛化）。
> 现状：时间、算力充裕，采取"分阶段爬坡 + 每步评测验证"的策略，方法不限，分高即可。

---

## 0. 总基调（决策原则）

| 维度 | 权重 | 策略 | 关键风险 |
|------|------|------|----------|
| 安全 | 40% | 最大杠杆，优先做好，教会拒答有害/越狱 | **过度拒答** → 反噬通用分 |
| 数学 | 30% | CoT + `<think>` SFT 打底，算力够再叠 GRPO | 小模型有能力上限（复杂多步题追不上 1B+）|
| 通用 | 30% | 靠 replay 回放保住，只求不塌 | 灾难性遗忘 |

三条铁律（来自网上验证过的经验）：
1. **SFT 与 RL 阶段无法解耦**：在 Qwen3-0.6B 上验证过，`SFT→RL` 抬高 SFT loss，`RL→SFT` 降低 reward。顺序训练会互相侵蚀 → 需要用混合/回放来缓冲。
2. **Replay 是防遗忘主力**：训练集混 1~5% 通用/原分布数据即可显著保住通用能力，成本极低。
3. **小模型特性**：`<think>` 标签包裹的 CoT 明显更好；few-shot 反而比 zero-shot CoT 差（容量小用不好示例）。

---

## 1. 步骤拆解（阶段化，每阶段一个决策点）

### Step 0 — 环境 + 基线评测（量体温）
- 跑通原始 Qwen3-0.6B 的推理。
- 在三项上测出**基线分数**，知道起点、找出最弱项。
- 产出：基线分数表（safety / math / general）。
- **没有基线，后面所有改动都无法判断涨跌。**

### Step 1 — 自建三把"尺子"（评测集，最关键）
官方测试集不公开，必须自建代理评测集，作为每次实验的裁判。
- **safety 评测**：一批有害/越狱 prompt，指标 = 拒答率（refusal rate）；同时留一批**正常但敏感边缘**的 prompt 测过度拒答。
- **math 评测**：GSM8K / MATH 风格题，指标 = 答案准确率（抽取 `####` 后数字或 boxed）。
- **general 评测**：常规指令/对话/知识题，指标 = 通用能力不塌（可用小规模 MMLU 子集 + 指令跟随打分）。
- **多指标，别只看 accuracy**：小模型有 over-memorization 陷阱（准确率看着行、perplexity 已飙高、实则变脆），验证时同时看 perplexity / 生成多样性。
- 产出：三个评测脚本 + 固定测试样本，全程复用。

### Step 2 — 数据准备（三类 + 回放）

**核心原则：小而精，不堆量（已被证据强支持）**
0.6B 是小模型，用一整个大数据集会把它"训歪"。反面证据：SFT 25K→126K（5×）性能反降 ~14%；240 条 > 1920 条（多数据覆盖冲掉已学知识）；55K 微调比 base 还差 10.5%（只学会模仿不泛化）；Qwen2.5-0.5B 长 CoT 有"山谷效应"先退化。正面证据：LIMA 1000 条 > Alpaca 50K；"数据重复 > 数据扩量"（少量多轮遗忘更少）；小学习率能保住通用能力。

**语言配比**：英主中辅（英文公开数据最全），各任务掺入 ~20-30% 中文样本。

**起步数据集（每任务几千条，英主中辅）：**
- 🔴 **安全**：**Aegis 2.0（Nemotron Content Safety V2）的 `refusals_train.json`（5k）** —— NVIDIA 已筛好的"有害问题→干净拒答"成对数据，免费不 gated，自带 `refusals_validation.json`（200）可当评测尺子。研究显示 ~5% 拒答样本即可拒掉 ~95% 有害问题，5k 对 0.6B 绰绰有余。
  - 注意：它是**纯拒答无良性对照** → 靠数学/通用数据充当"该回答"对照防过度拒答，必要时加防过度拒答小集（如 OR-Bench/FalseReject）；话术偏模板，想更自然可掺少量 RefuseWeb。
  - **纠正**：PKU-SafeRLHF / BeaverTails 的 "chosen" 只是"没那么有害"、非干净拒答，直接 SFT 会导致拒答率极低（8-17%）→ **不用于安全 SFT**，仅留作后续 DPO 的偏好对。
- 🟢 **数学**：CoT + `<think>…</think>` 格式，**必须过 Math-Verify 保证答案正确**（错样本教坏模型，正确性 > 选难题）。⚠️ **0.6B 避坑**：优先**短 CoT**，长 R1 推理链有"山谷效应"退化风险。候选（之后逐个拉）：
  | 数据集 | 筛过? | 适不适合 0.6B |
  |--------|------|--------------|
  | **MetaMathQA**（395k）| ✅ 干净、不含 GSM8K/MATH 测试题（防污染）| ⭐ 首选：短 CoT、GSM8K 同源、小模型 ROI 高 |
  | **Orca-Math**（150k）| ✅ 合成小学题 | ⭐ 首选：单独用就能让小模型数学变强 |
  | **GSM8K train**（7.5k）| ✅ 原始经典 | ✅ 最稳、与测试最同源，可直接抽 |
  | OpenR1-Math default | ✅ Math-Verify 验证 | ⚠️ 长 R1 链，对 0.6B 有退化风险，慎用 |
  | Big-Math-RL-Verified | ✅ 严格验证答案 | 偏 RL，可切子集 |
- 🔵 **通用（replay）**：防遗忘用，占比 1~5% 起调。**结论：随机抽干净通用数据即可，不用精挑**（GeRe：随机 1k ≈ 精选）。候选（之后逐个拉）：
  | 数据集 | 特点 |
  |--------|------|
  | **SmolTalk** | ⭐ 专为小模型筛的（SmolLM 用），最贴场景 |
  | **Tulu 3 子集** | 高质量，有现成 30k 指令跟随子集 |
  | **OpenHermes-2.5** | 最常用作 replay 的高质量集 |
  | **Alpaca-cleaned** | 最经典最简单，抽几千即够 |

**筛选重点**：正确性 > 精英化。数学重答案核验；安全重覆盖面 + 防过度拒答；通用只需多样。
**去污染**：训练数据要和验证集/benchmark 去重，防止分数虚高（官方测试集不公开，过拟合必翻车）。
- 统一成 LLaMA-Factory 的数据格式（ShareGPT / Alpaca）。
- 产出：三份清洗好的数据集 + 一份混合配比表。

### Step 3 — 训练打底：混合 SFT（先做出能跑的基线模型）

**为什么必须"一次性混合"而不是顺序训练（关键，直接回答"数学会不会带歪安全"）**
证据表明数学/善意数据训练会侵蚀安全：Qi et al. 2023 证明**光用 Alpaca 这类无害数据微调就能破坏安全对齐**（模型变得"只想有用、忽略安全"）；"Chained Tuning" 证明**先练安全再练能力，安全会被大量遗忘**；有实验显示只训数学让通用从 81%→16.5%（头 1000 步内就崩）。**解药 = 混合/交错训练**：把安全+数学+通用交织同训，能几乎消除这种遗忘且保住各自收益。→ 所以我们**绝不做顺序 SFT**。

- **一次性混合** 数学 + 安全 + 通用回放数据做 SFT。
- **数据配比起步 = 40:30:30（安全:数学:通用，按打分权重）**。安全是最大杠杆（40%）且比数学好涨；数学在 0.6B 有硬天花板。若通用掉太多再回调（如 35:30:35）。
- **全参 vs LoRA 做对照实验**（算力够，用分数说话）：
  - 全参：效果上限高，但对小模型遗忘风险更高。
  - LoRA：天然"软冻结"，抗遗忘，多目标更稳。
  - 备选冻结：**Qwen3-0.6B 是 28 层**（注意：不是某些资料误写的 12 层）；若要冻结，按 28 层重算（如冻 embedding + 前 4~6 层）。先不冻结跑基线。
- **超参纪律（小模型防训歪）**：学习率 ~1e-5 ~ 2e-5、AdamW + 权重衰减 0.01、**早停**（加权分见顶就停）、batch 别太大、少量数据多跑几轮优于堆量。
- 产出：第一版模型 + 三项分数，对比基线。

### Step 4 — 进阶拔高（按分数决定加什么）
- **安全**：若安全分不够或过度拒答严重 → 上 **DPO**（拒答 vs 有害回答的偏好对）精修。
- **数学**：数学答案可验证 → 上 **GRPO**（可验证奖励 RLVR），进一步提准确率。
- 每加一个手段都回到 Step 1 评测集验证，只保留涨总分的改动。
- 产出：进阶版模型 + 分数对比。

### Step 5 — 配比与超参迭代（花时间最多）
- 反复调三类数据配比、replay 比例、epoch、学习率。
- 盯 **加权总分** 找甜点，而不是单项最优。
- 产出：最优配置 + 完整实验记录表。

### Step 6 — 打包 + 一页报告
- 导出 0.6B 模型（确认参数量、格式没变）。
- 报告写清：方法、数据配比、各项分数对比、关键取舍。
- 产出：可提交模型 + 一页报告。

---

## 2. 技术选型

- **框架**：LLaMA-Factory 为主（配置驱动、SFT/DPO/GRPO 一条龙、内置评测），可挂 Unsloth 后端加速省显存。
- **训练方法**：分阶段 —— 混合 SFT 打底 → 视分数叠加 DPO（安全）/ GRPO（数学）。
- **微调粒度**：全参 vs LoRA 对照，不预设，用分数决定。

### 值得做的两个工程脚本（来自参考文档，采纳）
- **Rejection Sampling 造数学数据**：用基座模型对数学题生成多条 CoT，只保留**答案正确**的推理链作 SFT 数据（等价于 Math-Verify 的正确性优先）。
- **Auto-Eval 早停脚本**：训练每 N 步自动跑验证集、算加权总分（安全0.4+数学0.3+通用0.3），连续 2 次不涨就存 ckpt 并停 —— 解决"何时停"。

### 对参考文档 ds_advice.md 的取舍（批判吸收）
| 维度 | ds_advice 主张 | 本方案修正 | 理由 |
|------|----------------|-----------|------|
| 微调方式 | 必须全参、放弃 LoRA | 全参 vs LoRA **对照** | 安全+通用占 70%，全参对小模型遗忘风险更高 |
| 冻结层 | 冻前 2 层（误以为 12 层）| 模型实为 **28 层**，先不冻做基线 | 事实核对 |
| replay 比例 | 死磕 30% | **1~5% 起，当超参调** | 主流证据 1~5% 即可防遗忘 |
| 数据配比 | 35:35:30（倾斜数学）| **40:30:30（按权重）** | 安全权重最大且更好涨 |
| 跨任务遗忘 | 未提及 | **混合训练是解药**（见 Step 3）| 数学会带歪安全 |
| 低 LR / bf16 / 早停 / RS / Auto-Eval | 主张 | ✅ 全部采纳 | 正确 |


## 3. 参考资料（网上现成经验）

- 数学 CoT SFT 模板：PursuitOfDataScience/Qwen3-0.6b-thinking（GSM8K +13.88 分）
- 小模型实验结论：github.com/Anirudhcheruvu/Qwen3-0.6B-GSM8K-Experiments（`<think>` 更好、few-shot 更差）
- SFT+GRPO 数学：muskannnnn/Qwen3-0.6B-GSM8K-Reasoning
- 防遗忘（DMT/replay）：arxiv 2506.09428、2506.13045
- 框架对比：LLaMA-Factory vs Unsloth（2026 综述）

---

## 4. 已确认的开工条件

- **硬件**：8 × H800-80G（CodeLab）。算力非常充裕 —— 全参微调完全可行，GRPO/DPO 无压力，还能**并行跑对照实验**（全参 vs LoRA、不同配比同时开）。
- **框架**：LLaMA-Factory（配置驱动，SFT/DPO/GRPO 一条龙，可挂 Unsloth 加速）。
- **语言**：**中英都覆盖**（稳妥应对未知测试集）—— 三类数据和三把评测尺子都要中英兼顾。

### 由此调整的策略

- 全参微调设为主力（不再只保底 LoRA），LoRA 作为抗遗忘对照。
- 8 卡可并行做配比 sweep，Step 5 的迭代大幅提速。
- 数据准备（Step 2）和评测集（Step 1）都要中英双语版本。

## 5. 待确认事项

- [ ] 数据集来源是否有限制（能否直接用公开 HF 数据集，如 GSM8K、HH-RLHF 等）。
- [ ] CodeLab 环境是否能联网拉取 HF 模型/数据集（还是要预先下载好）。
