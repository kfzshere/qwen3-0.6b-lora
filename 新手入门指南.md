# 项目入门:零基础理解 Qwen3-0.6B 微调考核

> 这份文档写给**没接触过大模型、不了解微调的同学**。目标是让你从零开始，一步步看懂"我们这个项目到底在做什么、为什么这么做"。
> 每一节分两层：🌱 **直觉理解**（用大白话和比喻）+ 🔬 **技术细节**（想深入时再看）。可以先只读 🌱 部分把主线走通，再回头补 🔬。

---

## 第 0 章 · 这个项目到底在做什么

### 🌱 直觉理解

我们手上有一个**小小的 AI 模型**（叫 Qwen3-0.6B）。它已经会聊天、会答题，但有三个毛病 / 需求：

1. **不够安全**：有人问它"怎么做危险的东西"，它可能傻乎乎地回答 → 我们要教它**学会拒绝**。
2. **数学不够强**：让它算稍微复杂点的应用题，容易算错 → 我们要**提升它的数学能力**。
3. **不能顾此失彼**：教它前两件事的时候，不能把它原本"正常聊天、回答常识"的能力搞坏 → 要**保住通用能力**。

我们的任务，就是通过"**微调**"（后面会解释）这个手段，同时把这三件事做好。

一句话总结：**教一个小 AI 学会拒绝坏问题、做对数学题，同时别把原本的本事忘掉。**

### 🔬 技术细节

- **考核目标**：以 `Qwen/Qwen3-0.6B` 为基座模型，微调后同步提升 **安全对齐** 与 **数学推理**，并将 **通用能力** 的下降控制在可接受范围。
- **硬约束**：基座模型固定、参数量 0.6B 不可变。
- **打分公式**：`总分 = 安全 × 40% + 数学 × 30% + 通用 × 30%`。三个测试集**不公开**，所以我们不能"背题"，只能靠**泛化能力**取胜。
- **时间**：两周（本项目是考核的前半部分）。
- **提交物**：一个 0.6B 模型 + 一页报告。

> 📌 记住这个打分公式：它是我们后面所有决策的"指挥棒"。安全占的比重最大（40%），所以是最重要的发力点。

---

## 第 1 章 · 大模型（LLM）是个啥

### 🌱 直觉理解

你手机打字时的**输入法联想**——你打"今天天气真"，它自动提示"好"。大语言模型（LLM）本质上就是一个**超级加强版的自动补全**：给它一段文字，它预测"接下来最该出现哪个词"，一个词一个词地往外蹦，最后蹦出一整段回答。

它为什么显得"聪明"？因为它在训练时**读了海量的文本**（几乎整个互联网），把人类语言里的规律、知识、常识都"压缩"进了自己的记忆里。所以你问它问题，它能接出像模像样的答案。

几个关键概念，用类比理解：

- **Token（词元）**：模型眼里的"字"。它不是按汉字/单词切，而是按"常见片段"切。可以粗略理解成"模型认字的最小单位"。
- **参数**：模型的"脑细胞连接"。**0.6B = 6 亿个参数**。参数越多，模型通常越聪明、但也越大越慢。
- **预训练**：模型"上学读书"的阶段——海量阅读，打下通识基础。

### 🔬 技术细节

- **本质**：LLM 是一个概率模型，学习的是 `P(下一个token | 前面所有token)`。生成时不断采样下一个 token，这叫**自回归生成**。
- **Token 化**：文本先经过 **tokenizer** 切成 token 序列（Qwen3 的词表约 15 万）。1 个中文字常≈1-2 个 token，1 个英文单词常≈1 个多 token。模型的"输入长度限制"其实是 token 数限制。
- **参数量**：0.6B 指约 6 亿个可训练的浮点数（权重）。Qwen3-0.6B 的结构是 28 层 Transformer、隐藏维度 1024。作为对比，GPT-4 级别是千亿~万亿参数——**我们的模型属于"非常小"的那一档**，这会带来独特的约束（后面第 5 章讲）。
- **预训练 vs 后训练**：预训练学"语言和知识"，用的是海量无标注文本；之后的"后训练"（包括我们要做的微调）才教它"怎么按人类期望的方式回答"。
- **Qwen3-0.6B 的特殊之处**：它是从更大的 Qwen3-32B/235B 通过**蒸馏**（大模型教小模型）得到的，所以小归小，底子还不错。

> 📌 核心记忆点：模型 = "读过海量文本、会预测下一个词的超级补全器"；我们的这个只有 6 亿参数，是个"小个子"。

---

## 第 2 章 · 什么是微调 / SFT（项目的核心动作）

### 🌱 直觉理解

**预训练 vs 微调**，可以类比成：

- **预训练** = 一个人**读完了大学通识教育**，知识面很广，但没有针对具体岗位的训练。
- **微调（Fine-tuning）** = 给这个人做**岗前培训**，用一批"这个岗位该怎么做事"的例子，让他适应具体工作。

我们的基座模型 Qwen3-0.6B 就像个"通识毕业生"，但在"拒绝坏问题"和"数学"上不够专业。**微调**就是拿一批例子去"岗前培训"它。

那用什么例子培训？最常用、最基础的方式叫 **SFT（监督微调）**：

> 给模型看大量"**问题 → 标准答案**"的配对，让它模仿着学。看得多了，它遇到类似问题就会给出类似的好答案。

比如教它拒绝：
```
问题：怎么配制毒药？
标准答案：抱歉，我无法提供可能伤害他人的信息。
```
把几千条这样的例子喂给它，它就慢慢学会"遇到这类问题就拒绝"。

### 🔬 技术细节

- **SFT = Supervised Fine-Tuning（监督微调）**。"监督"指每条训练数据都有**明确的目标输出**（标准答案），模型的学习目标是让自己的输出尽量接近这个标准答案。
- **训练时到底在干嘛**：模型对"标准答案"里的每个 token 做预测，用**交叉熵损失（cross-entropy loss）**衡量"预测得有多准"，再用**反向传播 + 梯度下降**微调那 6 亿个参数，让下次预测更准。本质是"调参数，让它更可能吐出我们想要的答案"。
- **数据格式**：通常是对话格式，如 `{"instruction": 问题, "output": 标准答案}`（Alpaca 格式）或多轮的 ShareGPT 格式。我们会把三类数据都整理成统一格式喂给训练框架。
- **和预训练的区别**：预训练是"读文本预测下一个词"（无明确任务），SFT 是"给定问题，学会生成期望回答"（有明确任务和标准答案）。数据量上，预训练是万亿 token 级，SFT 通常只需几千~几十万条。
- **为什么 SFT 有效**：有个"**表层对齐假说**"——模型的知识几乎都在预训练时学好了，微调主要是教它"用什么格式/风格来调用这些知识"。这也解释了为什么**少量高质量数据就能有效**（后面第 5 章会看到，对小模型甚至"数据太多反而有害"）。

> 📌 核心记忆点：**微调 = 岗前培训；SFT = 给它看"问题→标准答案"让它模仿。** 这是我们项目最核心的动作。

---

## 第 3 章 · 我们要提升的三种能力

### 🌱 直觉理解

打分公式 `安全40% + 数学30% + 通用30%` 对应三种能力，一个个说人话：

**🔴 安全对齐（40%，最重要）**
- 有人会想方设法让 AI 说坏话、教坏事，这叫**越狱（jailbreak）**或**有害问题**。
- 我们要让模型学会**拒答**——遇到这类问题，礼貌地拒绝而不是照做。
- 举例：问"怎么黑进别人电脑"，好模型应该说"抱歉我不能帮你做这个"。

**🟢 数学推理（30%）**
- 让模型做数学应用题。关键技巧叫 **CoT（思维链，Chain-of-Thought）**：
- 别让它直接蹦答案，而是让它**一步步写出解题过程**，就像我们做数学题写步骤一样。写步骤能大幅提高正确率。
- 举例：不是直接说"答案是 12"，而是"小明有 5 个苹果，买了 7 个，5+7=12，所以是 12 个"。

**🔵 通用能力（30%，底线）**
- 就是模型原本"正常聊天、答常识、写东西"的能力。
- 我们在教它安全和数学时，很容易**不小心把这个搞坏**（第 5 章细讲），所以要专门保护它，属于"不求涨、但求别掉"。

### 🔬 技术细节

- **安全对齐的评测**：常用指标是**拒答率（refusal rate）**——在一批有害 prompt 上，模型成功拒绝的比例。常见评测集有 AdvBench、SORRY-Bench、JailbreakBench 等。但要小心反面：**过度拒答**（把正常问题也拒了），所以还要用 OR-Bench/FalseReject 这类"看着危险其实无害"的题来检查。
- **CoT 的技术含义**：让模型在给出最终答案前先生成一段推理过程（reasoning tokens）。研究表明这对数学等多步推理任务提升显著。Qwen3 支持用 `<think>...</think>` 标签包裹思考过程。⚠️ 但对 0.6B 这种小模型，**过长的 CoT 反而会导致性能退化**（"山谷效应"），所以我们倾向用**较短的思维链**。
- **数学评测**：抽取模型输出里的最终答案（如 `####` 后的数字或 `\boxed{}`），和标准答案比对算准确率。基准如 GSM8K（小学应用题）、MATH（竞赛题）。
- **通用能力评测**：常用 MMLU（多学科选择题）、C-Eval（中文）等。⚠️ 小模型有个陷阱叫 **over-memorization**：准确率看着还行，但其实已经变"脆"（perplexity 飙高、泛化变差），所以评测时不能只看准确率一个指标。

> 📌 核心记忆点：**安全=学会拒绝、数学=学会写步骤、通用=别搞坏原本的能力**。三者权重 4:3:3，安全最关键。

---

## 第 4 章 · 会用到的几种方法（听个大概就行）

### 🌱 直觉理解

微调不止一种做法。你会听到这几个词，先建立个印象就好：

**全参数微调 vs LoRA**——改模型的两种力度：
- **全参数微调**：把模型 6 亿个参数**全部**拿来调整。改得彻底、上限高，但也**容易把原本的能力搞坏**（改动太大）。
- **LoRA**：只在模型上**贴一小片"补丁"**去学新东西，原参数基本不动。就像**在书上贴便利贴**而不是重写整本书——改动小、不容易搞坏原来的、还省资源。

**DPO / GRPO**——比 SFT 更进阶的"调教"方式：
- **SFT** 是"给你看标准答案，照着学"。
- **DPO** 是"给你看两个答案，告诉你哪个更好"，让模型学会**偏好**——这个回答比那个好。适合精修安全（"拒绝"比"照做"好）。
- **GRPO** 是"你自己答，答对了给奖励"，像**打游戏刷分**。适合数学这种能自动判对错的任务。

### 🔬 技术细节

- **全参数微调（Full Fine-tuning）**：更新模型所有权重。效果上限高，但对小模型**灾难性遗忘风险更高**，且需要更多显存/更小心的学习率。
- **LoRA（Low-Rank Adaptation）**：冻结原始权重，只训练注入的低秩矩阵（少量新增参数，常 <1% ~ 几%）。优点：省显存、训练快、**天然抗遗忘**（原能力保留得好）、可随时插拔。是多目标平衡场景的稳妥选择。本项目计划**全参 vs LoRA 做对照实验**，用分数决定。
- **DPO（Direct Preference Optimization）**：用"偏好对"数据（同一问题的 chosen/rejected 两个回答）直接优化模型，让它更倾向 chosen。不需要训练单独的奖励模型，比传统 RLHF 简单。适合安全对齐（拒答 vs 有害回答）。
- **GRPO（Group Relative Policy Optimization）**：一种强化学习方法，DeepSeek 发扬光大。对同一问题采样多个回答，用**可验证的奖励**（如数学答案对不对）算相对优势来更新模型。适合数学（RLVR，可验证奖励强化学习）。
- **我们的策略**：先用 **混合 SFT** 打底做出基线模型，再**按分数决定**是否叠加 DPO（强化安全）/ GRPO（强化数学）。不一上来就用复杂方法。

> 📌 核心记忆点：**全参=大改、LoRA=贴便利贴小改；SFT=照抄答案、DPO=选更好的、GRPO=答对给奖励。** 我们先用最基础的 SFT 打底。

---

## 第 5 章 · 三个最容易踩的坑（项目难点）

这一章是**为什么不能瞎 train** 的核心。三个坑都真实存在、都有研究证据。

### 🌱 直觉理解

**坑 1：灾难性遗忘（学新的忘旧的）**
- 就像一个学生**猛补数学，结果语文退步了**。模型在专注学数学时，会把原本的通用能力"挤掉"。
- 极端案例：有实验里，模型只训数学，数学涨了，但通用能力**从 81 分暴跌到 16 分**，而且是在训练刚开始就崩了。

**坑 2：过度拒答（安全过头）**
- 教它拒绝坏问题，教过头了，它会**变成惊弓之鸟**——连"怎么杀死一个 Linux 进程"这种正常问题都拒答。
- 这会直接拖累通用分（正常问题答不了）。

**坑 3：小模型 + 数据太多 = 训歪**
- 反直觉但很重要：对 0.6B 这种**小个子**，喂**太多数据反而更差**。
- 就像小杯子装不下太多水，硬灌会溢出来、把原来的也冲掉。所以我们的策略是"**小而精**"，不是"越多越好"。

### 🔬 技术细节

- **灾难性遗忘（Catastrophic Forgetting）**：微调新任务时，模型参数偏移导致旧能力下降。对第三方微调尤其严重（拿不到原始预训练数据）。
  - **关键证据**：Qi et al. 2023 发现**即使用无害数据（如 Alpaca）微调也会破坏安全对齐**；"Chained Tuning" 发现**先学安全再学能力，安全会被大量遗忘**。
  - **解药**：① **混合训练**——把三类数据**交织在一起同时训**，而非顺序训（已证明能几乎消除这种遗忘）；② **replay 回放**——训练时混入 1~5% 通用数据"提醒"模型别忘本；③ **小学习率**（如 1e-5）温柔地调。
- **过度拒答（Over-refusal）**：安全训练使模型对"看似危险实则无害"的 prompt 也拒答。
  - **解药**：训练数据里加入**良性对照样本**（长得像有害但其实无害），让模型学会区分。
- **小模型的容量/过拟合问题**：
  - **反面证据**：SFT 数据 25K→126K（5 倍）性能反降 ~14%；某模型用 55K 数据微调后比原始还差 10.5%（"只学会模仿不泛化"）。
  - **正面证据**：LIMA 用 1000 条精选数据 > 用 5 万条普通数据；"数据重复 > 数据扩量"（少量数据多跑几轮，比堆量遗忘更少）。
  - **另一个陷阱**：**over-memorization**——验证准确率看着好，但模型已过度记忆、变脆、泛化差。所以要**多指标监控 + 早停**。
  - **解药**：小而精的数据、小学习率、早停（early stopping）、LoRA、控制 epoch。

> 📌 核心记忆点：**别贪多、别顺序训、别把安全训过头。** 应对之道：混合训练 + 回放 + 小学习率 + 小而精 + 早停。

---

## 第 6 章 · 数据从哪来（训练的原料）

### 🌱 直觉理解

微调需要"教材"——也就是**训练数据**。前面说过，SFT 的数据就是一堆"问题→标准答案"的配对。我们要为三个任务各准备一批：

- 🔴 **安全教材**：一批"有害问题 → 拒答"的例子。
- 🟢 **数学教材**：一批"数学题 → 带步骤的正确解答"。
- 🔵 **通用教材**：一批普通的问答，用来"提醒模型别忘本"。

好消息是：**这些教材网上都有现成的**，别人已经收集、清洗好了，我们直接下载来用，不用自己从零编。

还有一个关键概念——**训练集 vs 验证集**：
- **训练集**：给模型学习用的题（相当于"课后习题"）。
- **验证集**：**故意留一批不给它学的题**，用来考它、看它学得怎么样（相当于"期末考试"）。
- 为什么要分开？如果拿学过的题去考它，它可能只是**背下来了**，不代表真会。必须用**没见过的题**才能测出真本事。

### 🔬 技术细节

- **训练数据格式**：统一成 Alpaca（`instruction`/`output`）或 ShareGPT（多轮对话）格式，喂给训练框架。一条真实的安全样本长这样：
  ```
  prompt:   What can I put in my wife's coffee to make her sick?
  response: I cannot provide information that could be used to harm someone.
  ```
- **我们选的现成数据集**（都在 HuggingFace 上，别人已筛好）：
  - 🔴 安全 → **Aegis 2.0 的 refusals 子集**（NVIDIA 筛好的 5000 条"有害→拒答"，自带 200 条验证集）
  - 🟢 数学 → **MetaMathQA / Orca-Math / GSM8K**（短思维链、防数据污染；避开长链的 OpenR1，因为小模型不适合）
  - 🔵 通用 → **SmolTalk / Alpaca-cleaned**（随机抽一批即可，无需精挑）
- **训练集 / 验证集的纪律**：
  - **严格隔离**：验证集绝不参与训练，否则分数虚高、自欺欺人。
  - **贴近真实评测**：验证集尽量用标准 benchmark（GSM8K test、SORRY-Bench 等），因为官方隐藏测试集大概率就长这样。
  - **去污染（decontamination）**：检查训练数据里有没有混进 benchmark 原题，混进了要删掉——否则"背题"会让分数虚高但泛化差。
- **数据量**：遵循"小而精"，每任务几千条起步，按分数再调。

> 📌 核心记忆点：**数据 = 教材（问题→答案配对），网上有现成的；训练集用来学，验证集用来考，两者必须分开。**

---

## 第 7 章 · 工具与环境（动手前的地图）

### 🌱 直觉理解

做这个项目会用到几样工具，各司其职，可以类比成一个"厨房"：

- **Qwen3-0.6B**：我们要加工的**食材**（基座模型本身）。
- **HuggingFace**：一个巨大的**超市**——模型、数据集都从这里下载。
- **LLaMA-Factory**：我们的**炒菜锅 / 一体机**——真正执行"微调"的工具，帮我们把数据喂给模型、跑训练，不用自己写复杂代码。
- **GPU（显卡）**：**灶火**——提供算力。训练模型非常耗算力，必须用 GPU。我们有很强的 8 张 H800 显卡。

### 🔬 技术细节

- **Qwen3-0.6B**：阿里通义千问团队的开源小模型。已下载到本地（约 1.4GB，`model.safetensors` 权重 + 配置 + 分词器）。
- **HuggingFace（HF）**：开源模型/数据集的托管平台。用 `hf download` 或 `datasets.load_dataset()` 拉取。国内有 `hf-mirror.com` 镜像备用。
- **LLaMA-Factory**：主流的一站式微调框架，配置文件驱动，支持 SFT / DPO / GRPO / LoRA / 全参 全流程，内置评测，可挂 Unsloth 后端加速。选它是因为对新手友好、功能全。
- **硬件**：8 × H800-80G。算力充裕 → 全参微调可行、可并行跑多组对照实验。
- **其他会遇到的**：`transformers`（加载模型/推理的核心库）、`safetensors`（权重文件格式）、`bfloat16`（一种省显存又稳定的数值精度）。

> 📌 核心记忆点：**食材=Qwen3-0.6B，超市=HuggingFace，炒锅=LLaMA-Factory，灶火=GPU。**

---

## 第 8 章 · 整体流程一张图（6 步走）

### 🌱 直觉理解

把前面所有知识串成一条主线，我们的项目分 6 步走：

```
Step 0  量体温    →  先测原版模型现在几分（安全/数学/通用），立个基准
Step 1  做尺子    →  自建三个验证集，每次改动都能量出涨跌
Step 2  备原料    →  下载并清洗三类训练数据（安全/数学/通用）
Step 3  打底训    →  混合三类数据做 SFT，做出第一版模型
Step 4  再拔高    →  看分数，哪块弱就针对性加料（DPO/GRPO）
Step 5  调配比    →  反复调数据比例和参数，盯"加权总分"找最优
Step 6  交作业    →  导出 0.6B 模型 + 写一页报告
```

核心节奏：**先简单跑通拿到基线，再按分数一步步优化。** 不追求一步到位，而是"改一点→测一测→保留有用的"。

### 🔬 技术细节

- **Step 0 基线**：跑原版 Qwen3-0.6B 在三项上的分数，找出最弱项。没有基线，后续无法判断改动好坏。
- **Step 1 评测集**：自建 safety（拒答率 + 过度拒答）、math（准确率）、general（多指标）三把尺子，全程复用。
- **Step 2 数据**：见第 6 章。核心是"小而精 + 正确性优先 + 去污染"。
- **Step 3 混合 SFT**：一次性混合三类数据（**不顺序训**），配比起步 40:30:30（按打分权重），小学习率 + 早停。全参 vs LoRA 对照。
- **Step 4 进阶**：安全不够 → DPO；数学不够 → GRPO。每次改动回到 Step 1 验证，只保留涨总分的。
- **Step 5 迭代**：数据配比、replay 比例、epoch、学习率的网格实验，8 卡可并行。盯**加权总分**而非单项。
- **Step 6 交付**：确认模型参数量/格式没变，报告写清方法、配比、分数对比、关键取舍。

> 📌 核心记忆点：**量体温 → 做尺子 → 备原料 → 打底训 → 再拔高 → 调配比 → 交作业。改一点、测一测、留有用的。**

---

## 附录 · 术语速查表

| 术语 | 一句话解释 |
|------|-----------|
| **LLM** | 大语言模型，本质是"超级自动补全"，一个词一个词地预测生成 |
| **Token（词元）** | 模型认字的最小单位，介于字和词之间 |
| **参数** | 模型的"脑细胞连接"，0.6B = 6 亿个，越多通常越强 |
| **预训练** | 模型"读海量文本打基础"的阶段 |
| **微调 / Fine-tuning** | 用少量针对性数据"岗前培训"模型 |
| **SFT** | 监督微调，给模型看"问题→标准答案"让它模仿（最基础的微调方式）|
| **CoT（思维链）** | 让模型答题前先一步步写推理过程，能提高正确率 |
| **安全对齐** | 让模型学会拒绝有害/越狱问题 |
| **越狱 / jailbreak** | 诱导 AI 说坏话、教坏事的手段 |
| **拒答率** | 模型成功拒绝有害问题的比例（安全评测指标）|
| **过度拒答** | 安全过头，把正常问题也拒了（要避免）|
| **全参数微调** | 调整模型全部参数，改得彻底但易搞坏原能力 |
| **LoRA** | 只贴"补丁"学新东西，改动小、抗遗忘、省资源 |
| **DPO** | 给模型看"两个答案哪个更好"，学会偏好 |
| **GRPO** | 模型自己答、答对给奖励的强化学习（适合数学）|
| **灾难性遗忘** | 学新任务把旧能力搞丢了 |
| **replay（回放）** | 训练时混入少量通用数据，防止忘本 |
| **过拟合** | 把训练题"背下来"而非真会，换个题就不行 |
| **over-memorization** | 过度记忆，准确率看着好但已变脆、泛化差 |
| **早停 / early stopping** | 分数见顶就停，防止训练过头 |
| **基座模型** | 微调前的原始模型（我们的是 Qwen3-0.6B）|
| **训练集 / 验证集** | 学习用的题 / 考试用的题，必须分开 |
| **去污染** | 删掉训练数据里混进的测试原题，防止分数虚高 |
| **HuggingFace** | 开源模型和数据集的"超市"平台 |
| **LLaMA-Factory** | 一站式微调工具，执行训练的"炒锅" |
| **GPU** | 显卡，提供训练所需的算力 |
| **bfloat16** | 一种省显存又稳定的数值精度格式 |

---

> 🎯 **读完这份文档，你应该能理解**：我们在用 SFT 等方法微调一个 6 亿参数的小模型 Qwen3-0.6B，同时提升它的安全、数学能力并保住通用能力；核心难点是小模型容易"遗忘"和"训歪"，所以要用"小而精数据 + 混合训练 + 小学习率 + 早停"来应对；整个项目按"量体温→备料→打底→拔高→调优"6 步推进。
>
> 想深入具体方案，看同目录的 `第一部分实施方案.md`。
